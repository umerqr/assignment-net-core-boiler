/*
 * Challenges Messages
 *
 * This contains all the text for the Challenges container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Challenges';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Challenges container!',
  },
});
